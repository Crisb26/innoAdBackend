# Configuración para entorno de producción
spring:
  datasource:
    url: ${DB_URL}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
logging:
  level:
    root: WARN
    com.innoad: INFO

# En producción, habilitar los health indicators si están configurados
management:
  health:
    mail:
      enabled: true
    redis:
      enabled: true
